# ========================================
# 测试环境配置
# ========================================

# 数据库配置 (测试MySQL)
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
    url: jdbc:p6spy:mysql://192.168.0.245:3306/7link-pro?useSSL=false&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&serverTimezone=GMT%2B8&nullCatalogMeansCurrent=true&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD:mysql_5rpkQb}
    hikari:
      maximum-pool-size: 15
      minimum-idle: 3
      idle-timeout: 30000
      connection-timeout: 30000
      max-lifetime: 1800000

  # Redis配置 (测试Redis)
  data:
    redis:
      host: 192.168.0.245
      port: 6379
      database: 1
      timeout: 5000ms
      password: ${REDIS_PASSWORD:redis_FtTapD}
      lettuce:
        pool:
          max-active: 12
          max-idle: 6
          min-idle: 2
          max-wait: 2000ms

# Sa-Token配置 (测试环境)
sa-token:
  is-log: true
  is-color-log: false
  jwt-secret-key: test-secret-key-change-in-prod

# MyBatis配置 (关闭SQL打印)
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

# SpringDoc配置 (测试环境开启,方便测试)
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
